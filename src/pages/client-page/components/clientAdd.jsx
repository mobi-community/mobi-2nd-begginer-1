import styled from 'styled-components';

const ClientAdd = () => {
    return <Add>회원등록</Add>;
};

export default ClientAdd;

const Add = styled.h1`
    position: absolute;
    top: 35%;
    left: 50%;
`;
