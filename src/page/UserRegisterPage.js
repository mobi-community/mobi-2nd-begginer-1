import styled from "styled-components";

const UserRegisterPage = () => {
  return <Header>유저등록</Header>;
};

export default UserRegisterPage;

const Header = styled.h1`
  text-align: center;
  margin-top: 50px;
`;
